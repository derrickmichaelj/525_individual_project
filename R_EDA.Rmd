---
title: "R_EDA"
output:
  html_document: default
  pdf_document: default
date: "2026-03-28"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(arrow)
library(dplyr)
```

```{r}
df <- read_parquet("data/combined_weather.parquet")

dim(df)
head(df)
glimpse(df)
```

```{r}
nrow(df)
ncol(df)
summary(df)
```
